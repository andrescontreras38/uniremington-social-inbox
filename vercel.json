{
  "buildCommand": "npm run db:generate --workspace @uniremington/api && npm run db:deploy --workspace @uniremington/api && npm run build --workspace @uniremington/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/handler" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/internal/sync", "schedule": "0 7 * * *" },
    { "path": "/api/internal/retention", "schedule": "0 3 * * *" }
  ]
}
